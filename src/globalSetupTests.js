// const Enzyme = require('enzyme');
// const Adapter = require('enzyme-adapter-react-16');

// Enzyme.configure({ adapter: new Adapter() });

module.exports = async () => {
    console.log('globalSetupTests', 'once across');
    // Enzyme.configure({ adapter: new Adapter()});
}
